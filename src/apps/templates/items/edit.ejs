<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Update Item</title>
        <link href="/global.css" rel="stylesheet">
    </head>
    <body class="w-full h-auto">
        <div class="flex items-center p-40 flex-col space-y-10">
            <div class="flex items-center">
                <h1 class="uppercase font-bold text-2xl">Inventory Management System</h1>
            </div>
            <div class="w-[400px]">
                <form action="/items/<%= item._id %>/edit?_method=PATCH" method="POST" class="flex flex-col space-y-4">
                    <h1 class="uppercase text-xl text-green-600 font-bold">Update Item</h1>
                    <div class="flex flex-col space-y-2">
                        <label for="name" class="uppercase text-xs font-bold">Name</label>
                        <input id="name" name="name" type="text" value="<%= item.name %>" placeholder="Name..." class="px-2 py-1 outline-none border rounded shadow-sm" required/>
                    </div>
                    <div class="flex flex-col space-y-2">
                        <label for="category" class="uppercase text-xs font-bold">Category</label>
                        <input id="category" name="category" type="text" value="<%= item.category %>" placeholder="Category..." class="px-2 py-1 outline-none border rounded shadow-sm" required/>
                        <!-- <select name="category" class="px-4 py-2 rounded" required>
                            <option value="">Select Category</option>
                            <% categories.forEach((category, index) => { %>
                                <option id="cat_option_<%= index %>" value="<%= category %>" <%= item.category === category ? 'selected' : '' %>>
                                    <%= category %>
                                </option>
                            <% }) %>
                        </select> -->
                    </div>
                    <div class="flex flex-col space-y-2">
                        <label for="quantity" class="uppercase text-xs font-bold">Quantity</label>
                        <input id="quantity" name="quantity" type="text" value="<%= item.quantity %>" placeholder="Quantity..." class="px-2 py-1 outline-none border rounded shadow-sm" required/>
                    </div>
                    <div class="flex flex-col space-y-2">
                        <label for="price" class="uppercase text-xs font-bold">Price</label>
                        <input id="price" name="price" type="text" value="<%= item.price %>" placeholder="Price..." class="px-2 py-1 outline-none border rounded shadow-sm" required/>
                    </div>
                    <div class="flex flex-col space-y-2">
                        <label for="description" class="uppercase text-xs font-bold">Description</label>
                        <input id="description" name="description" type="text" value="<%= item.description %>" placeholder="Description..." class="px-2 py-1 outline-none border rounded shadow-sm" required/>
                    </div>
                    <button type="submit" class="px-5 py-3 text-xs font-bold uppercase bg-green-500 rounded text-white">
                        Update
                    </button>
                </form>
            </div>
        </div>
    </body>
</html>
